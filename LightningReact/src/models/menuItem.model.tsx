export interface MenuItemModel {
    id: number,       // question mark means optional
    name: string;
    description:string;
    price: number;
    image:string;
    category:string;
    restaurantId:number;
}